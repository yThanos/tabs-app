import { View } from "react-native"
import { Text } from "react-native-paper";

const settings = () => {
    return (
        <View>
            <Text>settings</Text>
        </View>
    )
}

export default settings